services:
  - type: web
    name: entry247
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python entry247_bot.py
    envVars:
      - key: PORT
        value: 10000
